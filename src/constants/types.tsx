export type Category = {
    id: number;
    name: string;
  };
  
export type Categoris = Category[];

export type Cat = {
    id: string;
    url: string;
    width: string;
    heigth: string;
  };
  
export type Cates = Cat[];